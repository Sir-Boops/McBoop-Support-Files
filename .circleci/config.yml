version: 2.1
jobs:
  build:
    docker:
      - image: alpine:3.10.3
    steps:
      - checkout
      - run: apk add py2-pip
      - run: pip install awscli
      - run: rm -rf .* 2> /dev/null
      - run: aws s3 sync . s3://boops-deploy/McBoop/Support-Files/
